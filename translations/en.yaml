configuration:
  log_level:
    name: "Log level"
    description: "Verbosity to use in logging. INFO is recommended, DEBUG will consume system resources and should only be used for debugging (Default: INFO)."
  cameras:
    name: "Cameras"
    description: "List of ONVIF cameras to monitor."
    fields:
      camera_name:
        name: "Camera Name"
        description: " A friendly camera name for configuration and logging."
      host:
        name: "Hostname/IP Address"
        description: "The hostname or IP address of the camera."
      port:
        name: "Port"
        description: "The ONVIF interface port number of the camera."
      username:
        name: "Username"
        description: "The username to log into the ONVIF interface of the camera."
      password:
        name: "Password"
        description: "The password to log into the ONVIF interface of the camera."
      ignore_ssl:
        name: "Ignore SSL"
        description: "Ignore SSL certificate validation errors (Default: False)"
      reconnect_time:
        name: "Reconnect Time (seconds)"
        description: "Seconds to wait before reconnecting (Default: 5, Minimum: 0)."
      idle_polling_interval:
        name: "Idle Polling Interval (seconds)"
        description: "Seconds to wait between polls while PTZ movement is idle (Default: 0.3, Minimum: 0)."
      active_polling_interval:
        name: "Active Polling Interval (seconds)"
        description: "Seconds to wait between polls while PTZ movement is active (Default: 0.3, Minimum: 0)."
      active_polling_threshold:
        name: "Active Polling Threshold"
        description: "The number of active polls with identical PTZ coordinates to determine that PTZ movement has stopped (Default: 2, Minimum: 2)."


      fast_poll_on_move:
        name: "Fast Poll on Movement"
        description: "Poll quickly during movement to send stop commands immediately (Default: False)."
